# Бэклог

Документ содержит ближайшие задачи проекта. Дорожная карта верхнего уровня описана в [roadmap.md](roadmap.md).

## Приоритеты

- `P0` — задача блокирует ближайший этап или без неё нельзя двигаться по основной линии разработки.
- `P1` — важная задача, которую нужно сделать в рамках ближайшего этапа, но она не блокирует первые шаги.
- `P2` — полезная задача, которую можно отложить без остановки основной разработки.

## Исполнители

- `DD` — Д. Д.;
- `NS` — Н. С.;
- `TBD` — исполнитель ещё не назначен.

## Актуальные задачи

| ID | Этап | Приоритет | Исполнитель | Задача | Критерий выполнения |
| --- | --- | --- | --- | --- | --- |
| INV-001 | 1 | P1 | NS | Проверить вызов оптимизатора из PETSc | Игрушечный пример из [INV-001](task-notes/INV-001-petsc-tao-pounders-smoke-test.md) минимизирует least-squares задачу с известным решением |
| BENCH-001 | 1 | P1 | NS | Подключить Google Benchmark | Простейший benchmark собирается и запускается |
| DOC-002 | 1 | P1 | NS | Описать первичный порядок сборки проекта | В `docs/user/` или `README.md` есть минимальные команды конфигурации, сборки и запуска тестов |
| PHYS-001 | 2 | P0 | NS | Реализовать температурные зависимости свойств скелета | Формулы Waples и Sekiguchi покрыты unit-тестами |
| PHYS-002 | 2 | P0 | NS | Реализовать эффективные теплофизические коэффициенты | Вычисляются `lambda`, `rho_c`, `A` для одно- и двухкомпонентных сред |
| PHYS-003 | 2 | P1 | NS | Реализовать табличную интерполяцию свойств флюида | Интерполяция покрыта тестами на константных и табличных данных |
| PHYS-004 | 2 | P0 | NS | Реализовать EASY%Ro | Аррениусовские интегралы и отражательная способность витринита проверены на заданной температурной истории |
| TEST-002 | 2 | P1 | DD | Добавить набор unit-тестов для автономных модулей | Тестами покрыты физика, интерполяция, EASY%Ro, BDF1 и базовые невязки |
| INV-002 | 3 | P0 | NS | Описать пространство калибруемых параметров | Параметры представлены проектным `Vector`; заданы нижние и верхние границы и взаимные преобразования физических параметров и нормированных координат `[0, 1]` |
| INV-003 | 3 | P0 | NS | Реализовать данные наблюдений и нормированные невязки | Наблюдаемые температуры и их погрешности хранятся отдельно от прямой физической задачи; вычисляются вектор нормированных невязок `r(theta)` и целевая функция `J(theta) = r(theta)^T r(theta)` |
| INV-004 | 3 | P0 | NS | Реализовать адаптер модельного прямого решателя для обратной задачи | Вектор параметров преобразуется в поверхностную температуру и базальный тепловой поток, а один вызов возвращает невязки, целевую функцию, статус и статистику прямого расчёта |
| INV-005 | 3 | P0 | NS | Подключить POUNDERS к модельной обратной задаче | После завершения `INV-001` оптимизатор работает через интерфейс целевой функции, учитывает границы параметров и не зависит от устройства прямого решателя |
| INV-006 | 3 | P0 | DD | Добавить сквозной тест восстановления параметров | Синтетические наблюдения формируются при известном скрытом наборе параметров, после чего обратный решатель восстанавливает их с заданной точностью |
| INV-007 | 3 | P1 | DD | Обработать отказы прямого расчёта внутри оптимизации | Неудачный прямой расчёт не прерывает оптимизацию аварийно; возвращаются согласованный штраф и диагностический статус |
| INV-008 | 3 | P2 | TBD | Добавить кэш вычисленных точек обратной задачи | Повторная оценка одинаковых параметров использует сохранённый результат; ключ учитывает параметры, версию физической задачи и влияющие на результат численные настройки |
| PERF-002 | 6 | P2 | TBD | Исследовать переиспользование факторизации в повторных прямых расчётах | После фиксации baseline измерена выгода от переиспользования портрета матрицы, символической или численной факторизации при неизменном операторе и меняющейся правой части |
| TIME-004 | 7 | P1 | DD | Реализовать BDF2 с постоянным и переменным шагом | Схема использует корректные коэффициенты для отношения соседних шагов, запускается через BDF1 и сбрасывает непригодную временную историю |
| TIME-005 | 7 | P0 | DD | Реализовать адаптивный выбор шага по времени | Контроллер использует оценку локальной ошибки, ограничения `dt_min` и `dt_max`, принимает или отклоняет шаг и точно попадает в заданные события |
| TIME-006 | 7 | P1 | DD | Реализовать автоматический выбор порядка BDF1/BDF2 | Порядок выбирается по оценке ошибки и доступной истории; после события или сброса истории интегратор безопасно возвращается к BDF1 |
| TIME-007 | 7 | P1 | DD | Реализовать повтор временного шага после отказа нелинейного решателя | Неудачный шаг отклоняется, повторяется с уменьшенным `dt`, не изменяет принятое состояние и завершается диагностическим статусом при достижении `dt_min` |
| NLS-003 | 7 | P1 | DD | Реализовать неточный метод Ньютона | Нелинейный решатель поддерживает постоянный параметр требуемой точности и стратегию Eisenstat–Walker и передаёт соответствующий допуск итерационному линейному решателю |
| LIN-005 | 7 | P1 | NS | Подключить GMRES с AMG-предобуславливателем | Реализован наследник `LinearSolver` на основе PETSc GMRES и одного выбранного AMG-варианта — hypre BoomerAMG или PETSc GAMG; возвращаются статус, число итераций и фактическая норма невязки |
| LIN-006 | 7 | P2 | DD | Исследовать специальные методы решения линейных систем | Подготовлена заметка об использовании дефляции и методов Фурье для систем с оператором Лапласа: описаны необходимые условия, применимость к задаче теплопереноса, сложность реализации и ожидаемые ограничения |
| INV-009 | 7 | P2 | DD | Исследовать сопряжённый способ вычисления градиента целевой функции | Подготовлена заметка с аналитическим видом действия сопряжённого оператора к производной Фреше прямого оператора на невязку и оценкой сложности реализации, численной стоимости и целесообразности подхода |
| TEST-004 | 7 | P1 | TBD | Проверить порядок временных схем и адаптивный шаг | На задачах с аналитическим решением проверены наблюдаемый порядок BDF1/BDF2, оценка локальной ошибки, принятие и отклонение шагов и точное попадание во времена событий |

## Выполненные задачи

### Этап 1. Каркас проекта

| ID | Приоритет | Исполнитель | Задача | Результат |
| --- | --- | --- | --- | --- |
| CORE-001 | P0 | NS | Создать базовую структуру C++-проекта | Есть CMake-проект, минимальный исполняемый файл собирается |
| DOC-001 | P0 | DD | Подготовить документационный каркас проекта | Созданы README, дорожная карта, процесс разработки, обзор архитектуры, changelog и структура `docs/` |
| CORE-002 | P0 | NS | Настроить CMake Presets | Есть общий `CMakePresets.json` для типовых конфигураций сборки |
| CORE-003 | P0 | NS | Подготовить vcpkg-манифест | Зависимости описаны в `vcpkg.json`, сборка использует manifest mode |
| INFRA-001 | P0 | NS | Настроить Docker-окружение для разработки | Есть Dockerfile с фиксированным инструментом (CMake, GCC/Clang, Ninja, vcpkg), проект собирается внутри контейнера |
| TEST-001 | P0 | NS | Подключить CTest и GoogleTest | Тестовый executable собирается и запускается через CTest |
| LIN-001 | P0 | NS | Проверить решение маленькой СЛАУ через UMFPACK | Игрушечный пример из [LIN-001](task-notes/done/LIN-001-eigen-umfpack-smoke-test.md) решает известную разреженную систему |
| INFRA-002 | P0 | NS | Настроить CI/CD пайплайн (GitHub Actions) | При PR и пуше в main автоматически запускается сборка и тесты в Docker |
| INFRA-003 | P1 | NS | Настроить Ruleset для ветки main (Trunk-Based Dev) | Запрещены прямые пуши в main, требуется обязательный прохождение CI и зеленый статус |

### Этап 2. MVP решателя прямой задачи

| ID | Приоритет | Исполнитель | Задача | Результат |
| --- | --- | --- | --- | --- |
| CORE-004 | P0 | DD | Описать временный формат входа для модельного прямого расчёта | Описан набор данных для расчёта на простой регулярной сетке: размеры области, сетка, литотип, начальная температура, граничные функции и точки наблюдений |
| CORE-005 | P0 | DD | Описать рабочее состояние прямого расчёта | Состояние хранит время и текущие физические поля. История временной схемы, рабочие векторы и стратегия рестарта принадлежат интегратору; накопленные величины EASY%Ro добавляются вместе с PHYS-004. |
| MESH-001 | P0 | DD | Реализовать простую регулярную тестовую сетку | Реализована неизменяемая регулярная сетка для модельной задачи: число ячеек, границы области, размеры ячеек и связанные с ними величины |
| CORE-006 | P0 | DD | Реализовать простую пространственную дискретизацию | Реализована консервативная FVM-дискретизация уравнения теплопроводности с постоянными коэффициентами и источником на регулярной сетке; поддержаны верхняя температура, нижний тепловой поток и нулевые боковые потоки, проверены CSR/CSC-сборка и второй порядок по пространству |
| LIN-002 | P0 | DD | Реализовать интерфейс линейного решателя | Есть общий интерфейс LinearSolver для решения `Ax = b`: внешний код передаёт матрицу, правую часть, вектор решения и параметры запуска, а получает статус и статистику. Интерфейс допускает подключение прямых и итерационных решателей без зависимости внешнего кода от их реализации |
| LIN-003 | P0 | DD | Реализовать типы `Vector` и `SparseMatrix` на основе Eigen | Реализованы обёртки над `Eigen::VectorXd` и разреженными матрицами Eigen; `SparseMatrix` поддерживает одно активное CSR- или CSC-представление, явное преобразование формата и сборку напрямую из проектных триплетов |
| LIN-004 | P0 | DD | Реализовать линейный решатель на основе UMFPACK | Реализован прямой `UmfpackLinearSolver`: решает сжатые CSC-системы через `Eigen::UmfPackLU`, возвращает детальные статусы и норму фактической невязки, покрыт unit-тестами |
| NLS-001 | P0 | DD | Реализовать базовый нелинейный решатель | Описаны интерфейсы нелинейной системы F(x)=0 и решателя: сборка невязки и линеаризации, параметры остановки и статусы результата |
| NLS-002 | P0 | DD | Реализовать итерационную логику нелинейного решателя | Реализован цикл Пикара/Ньютона с отдельными настройками линейного решения, PETSc-подобными критериями остановки, обработкой отказов и выбором линейного решателя через runtime-сеттер |
| TIME-001 | P0 | DD | Реализовать BDF1 для простой модельной задачи | Описаны интегратор, история, временная схема и BDF1, формирующая нелинейную задачу по истории шагов |
| TIME-002 | P0 | DD | Завершить реализацию BDF1 | BDF1 вычисляет производную `(xⁿ⁺¹ - xⁿ) / dt`, передаёт `derivative_shift = 1 / dt`, проверяет историю и допустимость шага времени |
| TEST-003 | P1 | DD | Проверить формулировку BDF1 | Unit-тестами проверены производная BDF1, новый момент времени, матрицы Пикара и Ньютона, `derivative_shift = 1 / dt` и ошибочные входные данные |
| TIME-003 | P0 | DD | Завершить базовый временной интегратор | Реализован фиксированный временной цикл: последний шаг точно попадает в `t_final`, в историю принимаются только сошедшиеся решения, результат содержит статус и числа принятых/отклонённых шагов; отказ нелинейного решателя сохраняет последнее принятое состояние |
| CORE-007 | P0 | DD | Собрать сквозной прямой расчёт на искусственном примере | Реализован повторно используемый модельный прямой решатель: фиксированная задача отделена от калибруемых поверхностной температуры и базального потока, полный временной расчёт возвращает статус, конечное поле и интерполированные температуры в точках наблюдения |
| PERF-001 | P2 | DD | Добавить минимальную статистику прямого расчёта | Результат прямого расчёта содержит полное wall-clock время, числа принятых и отклонённых шагов, суммарные числа нелинейных и линейных итераций, а также последние статусы решателей |
